# Brave Love

[![Version](https://img.shields.io/badge/version-1.2.1-ff5162.svg)](https://github.com/unclecat/WP_Brave/releases)
[![WordPress](https://img.shields.io/badge/WordPress-6.0%2B-21759b.svg)](https://wordpress.org/)
[![PHP](https://img.shields.io/badge/PHP-7.4%2B-777bb4.svg)](https://www.php.net/)
[![License](https://img.shields.io/badge/license-GPL%20v2%20or%20later-2ea44f.svg)](https://www.gnu.org/licenses/gpl-2.0.html)

一款为情侣记录而生的 WordPress 主题，用更统一的杂志感排版来承载你们的故事、照片、清单与日常。

![Brave Love Theme Preview](./screenshot.png)

当前稳定版：`v1.2.1`

完整用户手册：[`docs/USER-GUIDE.md`](./docs/USER-GUIDE.md)

Brave Love 不是单独的某一个页面模板，而是一套完整的情侣纪念站主题：

- `首页`：双计时器、今日天气、特别的日子、六大入口卡片
- `关于我们`：故事时间线，适合整理从相遇到现在的重要节点
- `点点滴滴`：按年份归档的时间轴文章
- `恋爱清单`：计划 / 已完成事项管理
- `甜蜜相册`：自动从点滴中汇总照片
- `随笔说说`：轻量短内容与前台快捷发布
- `祝福留言`：基于 WordPress 评论系统的公开留言板

如果你希望搭一个“能长期维护”的情侣网站，而不是只做一个静态落地页，这个主题就是为这个场景准备的。

## 核心能力

| 模块 | 作用 | 后台维护位置 |
| --- | --- | --- |
| 首页 | 展示恋爱时长、特别日子、天气与站点入口 | `外观 -> 自定义`、`设置 -> 天气城市`、`设置 -> 纪念日` |
| 关于我们 | 按时间顺序展示故事节点 | `关于我们` 文章类型 + 关于我们页面 |
| 点点滴滴 | 发布长文记录、地点、心情与摘要 | `点点滴滴` 文章类型 |
| 恋爱清单 | 记录想做的事和已完成事项 | `恋爱清单` 文章类型 + `清单分类` |
| 甜蜜相册 | 自动聚合点滴中的特色图与正文图片 | 无需单独录入，来源于 `点点滴滴` |
| 随笔说说 | 记录日常短句、心情和思念度 | `随笔说说` 文章类型，或前台快捷发布 |
| 祝福留言 | 收集亲友留言并审核显示 | `祝福留言` 页面评论 |

## 环境要求

- WordPress `6.0+`
- PHP `7.4+`
- 推荐开启固定链接
- 推荐使用 HTTPS 站点地址

## 安装方式

### 方式一：从 Release 安装
1. 在 [Releases](https://github.com/unclecat/WP_Brave/releases) 下载 `brave-love.zip`
2. 进入 WordPress 后台 `外观 -> 主题 -> 添加主题 -> 上传主题`
3. 上传并启用 `Brave Love`
4. 进入 `设置 -> 固定链接`，直接点击一次“保存更改”刷新重写规则

### 方式二：手动安装
1. 将主题目录放入 `wp-content/themes/`
2. 进入后台启用 `Brave Love`
3. 同样执行一次 `设置 -> 固定链接 -> 保存更改`

## 建站前先做这 7 步

1. 创建页面并分配模板：`首页 / 关于我们 / 点点滴滴 / 甜蜜相册 / 随笔说说 / 祝福留言`
2. 进入 `设置 -> 阅读`，把首页设为静态首页
3. 进入 `外观 -> 自定义 -> Brave 主题设置`，先配置 `首页与情侣`、`纪念日与倒计时`、`页面绑定与跳转`
4. 进入 `外观 -> 自定义 -> Brave 主题设置 -> 页脚导航`，设置页脚导航名称和链接
5. 进入 `设置 -> 天气城市`，添加你关心的城市坐标
6. 进入 `设置 -> 纪念日`，录入重要日子
7. 开始添加 `点点滴滴 / 关于我们 / 恋爱清单 / 随笔说说` 数据

## 必建页面与模板对应关系

| 前台页面 | 是否需要手动创建页面 | 使用模板 | 推荐说明 |
| --- | --- | --- | --- |
| 首页 | 需要 | `首页` | 创建后到 `设置 -> 阅读` 设为静态首页 |
| 关于我们 | 需要 | `关于我们` | 推荐别名 `about-us` |
| 点点滴滴 | 需要 | `点点滴滴` | 推荐别名 `moments` |
| 甜蜜相册 | 需要 | `甜蜜相册` | 推荐别名 `memories` |
| 随笔说说 | 需要 | `随笔说说` | 推荐别名 `notes` |
| 祝福留言 | 需要 | `祝福留言` | 页面评论需要保持开启 |
| 恋爱清单 | 不需要 | 无 | 这是 `love_list` 的归档页，默认链接为 `/lists/` |

## 页面功能与使用方法

### 1. 首页

用途：作为站点入口页，集中展示时间、天气、纪念日和各模块跳转入口。

如何启用：
1. 新建一个普通页面
2. 右侧模板选择 `首页`
3. 到 `设置 -> 阅读` 将它设置为静态首页

你需要维护的内容：
- `外观 -> 自定义 -> Brave 主题设置 -> 首页与情侣`
  - 恋爱起始时间
  - 首页正计时文案
  - 首页顶部导航副标题
- `外观 -> 自定义 -> Brave 主题设置 -> 纪念日与倒计时`
  - 下一个倒计时名称与时间
  - 首页倒计时文案
- `设置 -> 天气城市`
  - 控制首页天气模块是否显示
  - 支持添加任意数量城市，不限制 4 个
  - 支持在后台拖拽调整首页天气卡片顺序
- `设置 -> 纪念日`
  - 控制“特别的日子”卡片内容

前台表现说明：
- `恋爱时长` 会根据恋爱起始时间实时跳动
- `下一个纪念日` 倒计时会精确到秒
- `今日天气` 点击卡片会打开详情弹层，显示温度、空气质量、紫外线、穿搭建议与 6 小时趋势
- `特别的日子` 卡片按“离今天最近优先”排序，不按添加顺序排序
- 下方六张入口卡片会自动跳到站点各个核心页面

### 2. 关于我们

用途：展示完整的故事时间线，适合写“我们怎么认识、怎么走到一起、经历了什么”。

如何启用：
1. 新建一个页面
2. 模板选择 `关于我们`
3. 可在页面右侧 `当前页面 Hero` 中填写 Hero 标题 / 副标题 / 背景图

内容如何维护：
- 页面正文内容：作为顶部“写在前面”区域
- 时间线内容：通过后台左侧菜单 `关于我们` 中新增“故事节点”来维护

每个故事节点建议填写：
- 标题：节点标题，如“第一次见面”
- 摘要：使用 WordPress 原生 `摘要` 面板填写
- 正文：该节点的完整故事内容
- 特色图：节点插图
- 节点日期：控制时间线顺序
- 阶段标题：例如“相遇 / 靠近 / 稳定 / 未来”
- 关联点点滴滴：可选，前台会出现跳转入口

前台表现说明：
- 故事节点按 `_story_date` 正序排列，最早的在前面
- 如果摘要为空，卡片中不会显示摘要
- 如果设置了关联点滴，节点底部会出现“关联点点滴滴”链接
- 页面顶部会根据节点总数、首个节点日期、最新节点自动生成概览信息

### 3. 点点滴滴

用途：发布更完整的记录文章，是整站最核心的数据来源之一。

如何启用：
1. 新建一个页面
2. 模板选择 `点点滴滴`
3. 建议到 `外观 -> 自定义 -> Brave 主题设置 -> 页面绑定与跳转` 中指定这张页面

如何添加内容：
- 左侧菜单进入 `点点滴滴 -> 添加点滴`
- 每篇点滴建议填写：
  - 标题
  - 正文
  - 原生摘要 `Excerpt`
  - 特色图
  - 见面日期
  - 见面地点
  - 心情

摘要规则：
- 前台优先使用 WordPress 原生摘要 `post_excerpt`
- 如果原生摘要为空：
  - 列表页卡片会自动截取正文前 `120` 个词作为简介
  - 详情页不会单独显示摘要块

前台表现说明：
- 列表按 `见面日期` 倒序显示，新的在前面
- 可按年份筛选
- 作者头像会优先复用主题里设置的情侣头像
- 上传到正文或设置为特色图的图片，会自动进入 `甜蜜相册`

### 4. 恋爱清单

用途：记录“想一起完成的事情”和“已经完成的事情”。

如何启用：
- 不需要创建页面
- 这是一个文章类型归档页，默认访问路径为 `/lists/`
- 你可以在 `外观 -> 自定义 -> Brave 主题设置 -> 页脚导航` 里调整页脚导航名称和链接

如何添加内容：
- 左侧菜单进入 `恋爱清单 -> 添加事项`
- 可选设置 `清单分类`
- 每个事项建议填写：
  - 标题
  - 正文
  - 特色图
  - 是否已完成
  - 完成日期

前台表现说明：
- 顶部会自动统计：已完成、待完成、完成度
- 支持按分类筛选
- 支持按状态筛选：`已完成 / 待完成`
- 默认排序为：`待完成在前`，`已完成` 按完成日期从近到远排列
- 已完成事项如果填写了正文或特色图，卡片支持展开查看详情
- 未完成事项只显示简洁卡片信息
- 后台 `恋爱清单` 管理列表默认也会沿用同样的排序逻辑，方便维护

补充说明：
- `恋爱清单` 的 Hero 不走页面模板，而是在 `外观 -> 自定义 -> Brave 主题设置 -> 恋爱清单页（归档）` 里单独设置

### 5. 甜蜜相册

用途：集中浏览你们所有照片，不需要重复录入图片数据。

如何启用：
1. 新建一个页面
2. 模板选择 `甜蜜相册`
3. 建议在 `页面绑定与跳转` 中指定这张页面

它的数据来源：
- `点点滴滴` 的特色图
- `点点滴滴` 正文里的图片

前台表现说明：
- 自动按年份筛选
- 瀑布流布局显示
- 点击图片会进入 PhotoSwipe 灯箱
- 右侧信息面板可展示日期、地点、心情、摘要与原始点滴链接

可配置项：
- `外观 -> 自定义 -> Brave 主题设置 -> 列表与相册展示`
  - 每页照片数
  - 是否显示悬浮信息

注意：
- 新站点通常不需要手动维护这个页面的数据
- 只要持续发布 `点点滴滴` 并上传图片，相册会自动更新

### 6. 随笔说说

用途：发布轻量、碎片化的日常短内容。

如何启用：
1. 新建一个页面
2. 模板选择 `随笔说说`
3. 建议在 `页面绑定与跳转` 中指定这张页面

有两种发布方式：
- 后台发布：`随笔说说 -> 添加说说`
- 前台快捷发布：登录后直接在页面底部表单发布

后台可维护字段：
- 正文
- 心情表情
- 思念度（1-5 星）

前台表现说明：
- 按发布时间倒序显示
- 支持 `年份 -> 月份 -> 日期` 三级筛选
- 登录且有发布权限的用户，可以在前台直接发布说说
- 作者头像优先使用主题中配置的情侣头像

### 7. 祝福留言

用途：让亲友或访客给你们留言祝福。

如何启用：
1. 新建一个页面
2. 模板选择 `祝福留言`
3. 确保该页面 `评论` 是开启状态

你需要知道的规则：
- 访客通过页面底部表单提交昵称、邮箱和祝福内容
- 所有新留言默认进入审核后才显示
- 前台只显示 `已审核` 留言

前台表现说明：
- 留言按时间倒序显示
- 支持按年份筛选
- 使用主题内置头像池，不依赖外部头像服务
- 如果页面关闭评论，底部表单会自动隐藏

## 后台入口说明

### 外观 -> 自定义 -> Brave 主题设置

这里是主题的总控台，建议按下面的理解来维护：

- `首页与情侣`
  - 改首页恋爱正计时、首页顶部导航副标题、情侣头像和昵称
  - `男生 / 女生作者用户 ID` 只影响 `点点滴滴`、`随笔说说` 里作者样式映射，不是必填
- `默认页面 Hero`
  - 这里只放内容页通用的默认 Hero 背景
  - `关于我们 / 点点滴滴 / 甜蜜相册 / 随笔说说 / 祝福留言 / 旅行计划` 如果某一页想单独改标题、副标题或背景，请去页面编辑器右侧的 `当前页面 Hero`
- `纪念日与倒计时`
  - 负责首页顶部倒计时：名称、时间、文案
  - 也负责首页“特别的日子”模块标题
  - 真正的纪念日列表内容不在这里维护，而是在 `设置 -> 纪念日`
- `页面绑定与跳转`
  - 用来绑定站点实际使用的普通页面
  - 首页入口、页脚默认链接、主题内部跳转都会优先跟随这里
  - `恋爱清单` 不在这里配置，因为它是归档页，不是普通页面
- `首页入口卡片图标`
  - 只改首页 6 张入口卡片的 Emoji 图标
  - 不改标题、副标题和链接
- `恋爱清单页（归档）`
  - 单独设置恋爱清单归档页的 Hero 标题、副标题、背景图
- `列表与相册展示`
  - 控制点点滴滴每页数量、甜蜜相册每页数量、相册悬浮信息显示
- `页脚导航`
  - 拖拽调整顺序
  - 单独改名称和链接
  - 链接留空时会自动继承 `页面绑定与跳转`
- `页脚访客统计`
  - 控制是否显示页脚访客统计
  - 可改今日 / 累计访客前后缀
  - 手动覆盖数字只用于校正数据，平时不需要填写
- `自定义代码`
  - 放自定义 CSS
  - 放页脚统计脚本、站点验证代码等

### 设置 -> 天气城市

作用：维护首页天气模块。

可做的事：
- 开关天气模块
- 添加任意数量城市
- 为每个城市填写名称、纬度、经度
- 通过拖拽左侧手柄调整首页天气卡片顺序

填写建议：
- 坐标格式使用十进制度，例如 `31.2304` / `121.4737`
- 主题天气数据使用 QWeather API Key，可直接在 `设置 -> 天气城市` 中填写 `QWeather API Host` 和 `QWeather API Key`
- 如果服务器环境里也配置了 `QWEATHER_API_HOST` 和 `QWEATHER_API_KEY`，服务器配置优先，后台保存值作为兜底
- 如果天气不显示，优先检查坐标是否填写正确

### 设置 -> 纪念日

作用：维护首页“特别的日子”卡片。

可做的事：
- 添加纪念日名称
- 选择纪念日日期
- 删除或调整纪念日条目

排序说明：
- 前台不是按添加顺序显示
- 而是按“离今天最近优先”排序

### 点点滴滴 -> 相册数据管理

仅旧站迁移用户需要关注。

作用：处理旧版 `memory` 数据。

支持：
- 将旧 `memory` 转换为 `moment`
- 删除旧相册数据

如果你是新装站点，可以忽略这个页面。

### 点点滴滴 -> 摘要迁移

仅曾经使用旧版 `_moment_summary` 自定义摘要字段的站点需要关注。

作用：把旧摘要迁移到 WordPress 原生摘要 `post_excerpt`。

推荐流程：
1. 先点击“迁移到原生摘要”
2. 确认前台显示正常
3. 再点击“清理已迁移旧字段”

新装站点通常不需要执行这个页面的操作。

## 内容模型与字段一览

| 内容类型 | 主要字段 | 用途 |
| --- | --- | --- |
| `moment` | 标题、正文、摘要、特色图、见面日期、地点、心情 | 点点滴滴主内容，同时为相册提供图片数据 |
| `story_milestone` | 标题、摘要、正文、特色图、节点日期、阶段标题、关联点滴 | 关于我们时间线节点 |
| `love_list` | 标题、正文、特色图、是否已完成、完成日期、分类 | 恋爱清单事项 |
| `note` | 正文、作者、心情表情、思念度 | 随笔说说 |
| `page` Hero 元信息 | Hero 标题、Hero 副标题、Hero 背景图 | 用于关于我们 / 点点滴滴 / 相册 / 说说 / 留言等页面头图 |

## 常见操作说明

### 如何修改页面头图和文案？
- 普通页面：进入对应页面编辑页，使用右侧 `当前页面 Hero`
- 恋爱清单：进入 `外观 -> 自定义 -> Brave 主题设置 -> 恋爱清单页（归档）`

### 如何让“关于我们”显示摘要？
- 在故事节点编辑器中打开并填写 WordPress 原生 `摘要` 面板
- 如果摘要为空，前台不会显示摘要

### 如何让“点点滴滴”显示摘要？
- 同样使用编辑器自带的 `摘要` 面板
- 如果摘要为空，列表页会自动截取正文，详情页不额外显示摘要块

### 为什么我上传的图片出现在甜蜜相册里？
- 因为相册会自动聚合 `点点滴滴` 的特色图和正文图片
- 这是设计如此，不需要再单独上传一次

### 为什么祝福留言页看不到表单？
- 请检查该页面是否开启评论
- 也请检查 `设置 -> 讨论` 是否阻止了评论功能

### 为什么首页天气没有显示？
- 检查 `设置 -> 天气城市` 是否已启用天气模块
- 检查城市是否填写了正确的经纬度
- 检查 `wp-config.php` 或环境变量里是否已配置 `QWEATHER_API_HOST` 与 `QWEATHER_API_KEY`
- 检查首页是否确实使用了 `首页` 模板

## 推荐初始化顺序

如果你是第一次搭站，推荐按下面顺序配置：

1. 启用主题
2. 创建 6 张页面并分配模板
3. 设置首页为静态首页
4. 在 `自定义` 中配置 `首页与情侣`、`纪念日与倒计时`、`页面绑定与跳转`
5. 添加天气城市与纪念日
6. 先录入几篇 `点点滴滴`
7. 再录入 `关于我们` 的故事节点
8. 最后补充 `恋爱清单`、`随笔说说` 与 `祝福留言`

这样可以最快看到一个完整可用的网站雏形。

## 目录结构

```text
brave-love/
├── assets/                  # CSS / JS / fonts / vendor
├── docs/                    # 用户使用手册与后续补充文档
├── inc/                     # CPT、Customizer、Meta Box、后台页、工具函数
├── page-templates/          # 首页与主要内容页模板
├── template-parts/          # 通用模板片段
├── style.css                # 主题头信息（前端样式由 assets/css/* 注册）
├── functions.php            # 主题入口与资源注册
├── screenshot.png           # WordPress 后台主题截图
├── README.md                # 项目说明
├── CHANGELOG.md             # 更新日志
├── RELEASE.md               # 当前版本发布说明
├── RELEASE-CHECKLIST.md     # 发版清单
└── TEST-REPORT.md           # 最近一次发版前测试报告
```

## GitHub 与发布信息

- Repository: <https://github.com/unclecat/WP_Brave>
- Latest release: <https://github.com/unclecat/WP_Brave/releases>
- Release asset: `brave-love.zip`
- User guide: [docs/USER-GUIDE.md](./docs/USER-GUIDE.md)
- Author site: <https://www.1ink.ink/>

## 版本说明

当前 `v1.2.1` 已包含：

- 首页天气已切换到 QWeather API Key 方案，详情弹窗支持空气质量、空气预报、贴心提醒与今日降雨概率
- 首页天气 REST 新增 5 分钟“整包快照缓存”，前台优先读服务器快照，短时接口波动时会优先回退到上一份成功快照
- 首页天气城市后台支持拖拽排序，保存后前台按拖拽顺序展示
- 首页天气后台现已支持直接填写 `QWeather API Host / API Key`，不必强依赖修改 `wp-config.php`
- 天气模块已拆分为 `inc/weather/*.php` 多文件结构，后续维护配置、接口、文案和 REST 输出更清晰
- 主题主样式已拆分为 `assets/css/theme-core.css` + `assets/css/brave.css`，`style.css` 仅保留主题头信息，便于后续维护
- `Customizer` 与通用 helper 已拆分为多个 `inc/*` 子模块，后续排查和扩展更清晰
- `Customizer` 后台分组已按实际使用逻辑重组，并补充了“作者映射 / 默认页面 Hero / 页脚导航 / 访客统计”等分组说明
- 恋爱清单前台支持按完成状态与完成日期自动排序，后台管理列表默认同步相同逻辑
- 最近几个 patch 还额外清理了旧分钟降雨链路，并把天气请求坐标精度恢复到 4 位小数

如果你推送新的 tag，仓库中的 GitHub Actions 发布流程会自动构建 Release 资产。

## License

GPL v2 or later
